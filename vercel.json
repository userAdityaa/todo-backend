{
    "version": 2,
    "builds": [
        {
            "src": "cmd/*.go",
            "use": "@vercel/go"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/cmd/main.go"
        }
    ]
}